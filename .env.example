DATABASE_URL=postgresql+psycopg2://active:active@postgres:5432/active_defense
REDIS_URL=redis://redis:6379/0
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM=noreply@active-defense.local
# Brevo production example (uncomment and fill; remove docker-compose SMTP_HOST=mailhog override to use in Docker):
# SMTP_HOST=smtp-relay.brevo.com
# SMTP_PORT=587
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false
# SMTP_USER=you@example.com
# SMTP_PASSWORD=xsmtpsib-your-brevo-smtp-key
# SMTP_FROM=noreply@yourdomain.com
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=false
SMTP_USE_SSL=false
ML_RISK_URL=http://localhost:8082
ML_API_KEY=
ML_FACET_MODE=false
SESSION_SECRET=change-me-in-production
SEED_ADMIN_PASSWORD=Admin123!
SEED_DEMO1_PASSWORD=Demo123!
SEED_DEMO2_PASSWORD=Demo123!
LINE_MFA_ENABLED=false
LINE_CHANNEL_ACCESS_TOKEN=
LINE_PUSH_API_URL=https://api.line.me/v2/bot/message/push
IP_BLOCK_TTL_SECONDS=300
MEDIUM_THRESHOLD=0.4
HIGH_THRESHOLD=0.7
BLOCK_THRESHOLD=0.9
BASELINE_DEVIATION_THRESHOLD=0.35
TRUST_PROXY_HEADERS=false
RATE_LIMIT_LOGIN_PER_MIN=30
RATE_LIMIT_MFA_SEND_PER_MIN=5
RATE_LIMIT_MFA_SEND_PER_CHALLENGE=3
RATE_LIMIT_MFA_VERIFY_PER_MIN=10
MFA_OTP_TTL_SECONDS=300
MFA_MAX_ATTEMPTS=3
# APP_DEBUG=true  → OTP via Mailhog (http://localhost:8025); login response includes debug_otp
# APP_DEBUG=false → send real email using SMTP_* below (e.g. Brevo)
APP_DEBUG=true
# Auto-send OTP when MFA is triggered (risk scoring still decides whether MFA is required)
MFA_AUTO_SEND=true
# true = MFA for every non-blocked login; false = MFA only on high-risk logins
MFA_ALWAYS_REQUIRED=false
COOKIE_SECURE=false
BASE_URL=http://localhost:8000
NYCU_OAUTH_CLIENT_ID=
NYCU_OAUTH_CLIENT_SECRET=
NYCU_OAUTH_HTTP_TIMEOUT_SECONDS=60
LINE_LOGIN_CHANNEL_ID=
LINE_LOGIN_CHANNEL_SECRET=
LINE_LOGIN_CALLBACK_URL=
LINE_OFFICIAL_ACCOUNT_URL=
FRONTEND_BASE_URL=http://localhost:8000
REGISTRATION_SESSION_TTL_SECONDS=900
RATE_LIMIT_REGISTER_PER_MIN=20
